import { createTheme } from "@mui/material";

const friendsTheme = createTheme({
  palette: {
    primary: {
      main: "#fff6f6",
      dark: "#f3dade",
    },
    text: {
      primary: "#000",
      secondary: "#fff",
    },
    background: {
      default: "#B798FF",
    },
  },
});

export default friendsTheme;
